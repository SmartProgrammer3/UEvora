package PrimeiroMiniTestePratico;
//Retirar os comments para verificar quais é que são as conversões válidas e inválidas 
/*
 
public class Ex1 {
    public static void main(){
        float t=0.0f , w= 0.0f;
        int i= 0 , j=0;
        float p = 12.8f , q = 10.0f;
        int a=2, b=5, k=3;

        p = Math.exp((a*a) - 4.0*b*k ); //Invalido
        t = p/a;
        w = (int)p / Math.max(a,b);
        w = (float)Math.sin(Math.hypot(a,2));
        j = ((int) p +q) / (a%2); //Invalido
        j = (k-b) / (a * (int)Math.sqrt(p));
        t = w+(i*j);
        a = a+p; //Invalido
        p = ((p/a)/q)/b;
        w = ((float)Math.round(q))/k;
    }
}


 */